name: Test Self Hosted Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check Runner
        run: |
          echo "Hello from my self-hosted runner"
          hostname
          whoami
